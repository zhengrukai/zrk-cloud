# CLAUDE.md

## 项目说明
这是一个 Spring Boot 项目。

## 目录说明
- `.claude/skills/`：执行规则文件，Claude Code 按需读取

## 强制语言规范
1. 提取核心逻辑，转化为“2-4字短前缀 + 冒号 + 极简动宾短语”。
2. 只保留名词和动词（做什么、存什么、调什么），**干掉所有形容词、副词和业务修饰语**（如“体验好”、“极其稳定”、“无缝接入”）。
3. 能用词组绝不用完整句子，能用顿号绝不用逗号。保持极度克制和扁平化。

## 全局规则
- 不主动打开任何业务代码文件，除非用户允许
- 若调用工具失败，则不要擅自找其他方式代替，直接停止告知用户
- 需要查看某文件时，先说明原因，询问用户是否提供
- 输出只陈述事实，不写结论和建议

## 文件变更规则
**任何新增或修改文件，必须遵守以下流程，不得跳过：**
1. 说明将要变更的文件路径和变更内容
2. 询问用户是否确认
3. 用户确认后，通过 IntelliJ diff 视图展示变更内容
4. 等待用户在 IDE 中手动点击 Apply 按钮后才视为生效
5. 未经用户 Apply，不得进行下一步操作

## Skills
- @.claude/skills/

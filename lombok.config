# 配置作用域：仅当前目录及子目录生效，不向上冒泡
config.stopBubbling = true
# 全局toString包含父类
lombok.tostring.callsuper=CALL
# 全局equals/hashCode包含父类
lombok.equalsandhashcode.callsuper=CALL
# 全局setter支持链式调用
lombok.accessors.chain=true
